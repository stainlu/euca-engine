[package]
name = "euca-reflect"
version.workspace = true
edition.workspace = true

[dependencies]
euca-reflect-derive = { path = "../euca-reflect-derive" }
serde_json = { version = "1", optional = true }

[features]
default = []
json = ["dep:serde_json"]
